[package]
name = "nethack-babel-audio"
version.workspace = true
edition.workspace = true

[dependencies]
rodio = "0.20"
nethack-babel-engine = { path = "../engine" }

[dev-dependencies]
hecs = { version = "0.10", features = ["serde"] }
